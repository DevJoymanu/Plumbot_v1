# Follow-ups, cron and cancelling

<!-- Moved verbatim from the root CLAUDE.md "Current State" section. The root file
now carries only the rules that apply everywhere and points here; the nested
CLAUDE.md files point here for the directory they sit in. Keep this file the
single home of the detail below, and update it in the same commit as the code. -->

send_followups (the cap, the floor, the owner scripts, placement), the Railway cron trap, and the staff stop/cancel controls.

### Follow-ups & cron (`bot/management/commands/`)
- `send_followups` — **FOUR is a CEILING, and FOUR HOURS is a FLOOR** (owner rule, 2026-09-09, superseding "four for every lead" of 2026-09-03 where the two collide). Three rules, in one place each: (1) at most `FOLLOWUP_CAP_PER_REPLY` = 4 touches between one message of the lead's and the next, counted by `touches_since_last_reply` off the TRANSCRIPT rather than any one loop's counter (each loop keeps its own — `followup_count`, and the two nudge states in `internal_notes` — and none can see the others, so a lead chased four times and then parked could be nudged four more times off the same silence; a `[MANUAL FOLLOW-UP]` by a human is deliberately not counted, since the cap is about the machine talking over itself). It is asked FIRST in `_is_ready_for_followup` and again in both nudge loops. (2) `FOLLOWUP_MIN_GAP_HOURS` = 4.0, and `LAST_CALL_MIN_GAP_HOURS` now EQUALS it — the old 0.75h last-call relaxation is gone, because a minimum is a minimum. `space_offsets` is the single spacing resolver, shared by the main schedule and both nudge loops: it pushes a close pair apart and **DROPS what no longer fits rather than squeezing it**, so when the gap and the count cannot both hold, the COUNT gives. (3) Every touch is contextual — see the fallback templates below. **THE CONSEQUENCE:** with the old two blocks (~5.5 sendable hours) a standard 24h lead got 1 to 3 touches, usually 2. `CONTACT_WINDOWS` is now ONE block, 08:03-20:33 (owner rule, 2026-09-21), so the 4h floor allows three sends a day and a 24h lead can reach its four again when it writes early enough; a night still costs a touch. The knob is still `CONTACT_WINDOWS`, and the helpers take a list, so a second block can come back without code changes. The count is read off the schedule everywhere (`max_followups_for` = `len(followup_offsets_for(lead))`), so the UI chip, the dashboard due-list, the LLM prompt and cron retirement all follow it without being told.
- **The follow-up SCRIPTS are the owner's own April 2026 copy** (owner decision, 2026-09-19, reversing the 2026-09-09 contextual rewrite). `service_type`, `area`, `availability` and `complete` in `_template_message` are the owner's words, verbatim apart from " // " becoming a blank line and each dash becoming the stop or comma it stood for. That includes "We're getting booked up this week", "tight on slots", "We price the job upfront", "The price is fixed once we confirm" and "Still looking for a plumber?". **The owner chose these knowingly; do not "fix" them back**, and change them only when asked. The `owner script` cases in TEST 0 pin every line word for word. Three things were kept around the script on the owner's say-so: `project_description` keeps the contextual copy (names their job; `plan_or_visit` was offered and declined), the `ad_service` bank for bathroom-ad leads, and the `price_tiedown` bank. `{service}` is the service TYPE in words ("bathroom renovation", else "plumbing project"), never free text, and "around Harare" is the tenant's own `location_city`, omitted when blank. The scripts go out whole; only the MODEL's rewrite is held to them (`fit_to_template`: more questions than the script means the script goes out instead, and trailing lines after the last question are cut when the script ends on one). Note the AI path still rewrites the script on every send when DeepSeek is up, and sampled drift was about 1 in 5.
- **Audited against the live sends of 2026-09-18/19** (17 leads). Each rule below is a follow-up that really went out: (1) **the ad they clicked is read** (`ad_subject`, off `ctwa_referral` headline/body). All eleven barmak ad leads came from one BATHROOM ad and were asked "is it a bathroom, a kitchen, or a new installation?"; one replied "I thought you were selling tubs, looks like it's not" and left. They now get the `ad_service` bank ("Is it the whole bathroom you want redone, or one thing in it, like the tub or the shower?"), and the AI prompt's rephrase-the-last-question block is off for them, because it handed the model the old room question to reword. (2) **An unanswered price tie-down is the open question** (`pending_price_tiedown`, reading our own follow-ups too, so the run stays on it), asked alone and named for the thing priced, never merged into an area ask ("Which suburb are you in? Also, does the starting price range sit alright with your budget?"). It keeps the ladder's signature, so a "no" to it climbs the budget ladder. (3) **One question, and the touch ends on it** (`end_on_first_question`, applied to every send in `_process_lead`), which also cuts the model's closing filler. (4) **Not every conversation is a lead** (`not_a_lead_reason`, asked in `_is_ready_for_followup`): a vendor pitching us (`is_inbound_sales_pitch`) and an `[OOS_PENDING] category=out_of_scope` message are not chased; a known job always is. (5) **Shona leads get Shona** (`lead_writes_shona`, AI-primary, since the keyword detector needs two markers; only the AI path writes Shona, the offline templates are still English). (6) **Copy that contradicted the conversation is gone**: "before I can get you a price" to a lead already priced, "earlier in the week or later?", "What time works?" to a lead with a time set, and a paragraph of description pasted in as the job (`job` falls back to the service label past six words). A description now answers the service question, as `_job_is_known` does in the chat. Also fixed on the way: `extraction_mixin` stored the classifier's display label "Bathroom Renovation" raw because its check was case-sensitive, so every reader keyed on `bathroom_renovation` missed it; `_service_label` tolerates the rows already stored. Pinned by the `followup audit` cases in TEST 0. **Still open:** the opener an ad lead gets ("Hello, How may we assist you on plumbing services", `COLD_OPENER`) ignores the ad too, and that is where these conversations stall.
- Placement, unchanged except where the floor now truncates it. On a full 72h window the touches take the band placement: **2 in the first 24h, 1 in 24-48h, 1 in 48-72h**, measured from the lead's last message — `FOLLOWUP_BAND_OFFSETS` as absolute hours per temperature (COLD 6/13/33/60h, VERY_HOT 4/10/27/51h), with `FOLLOWUP_BANDS` the contract in data and TEST 0 asserting every tuple against it. **Temperature moves a touch WITHIN its band, never into another one.** **Only a 72h window can carry that placement** — a touch written for 33h could only ever bounce with 131047 on a window that shut at 24h — so any shorter window (a standard lead, or an ad lead who replied with most of the 72h spent) gets `SHORT_WINDOW_FRACTIONS` instead: the same four touches as a share of the usable span (24h COLD ≈ 3.6/8.6/13.5/18.9h, 48h left ≈ 7.4/17.7/27.9/39.1h). Fractions rather than a second hour table, so one branch covers every window length. `followup_offsets_for(lead)` is the resolver and picks the branch on `bands[-1] <= usable_window_hours`; **the count is read off it** — `max_followups_for` is `len(followup_offsets_for(lead))`, so cron retirement, the UI chip, the dashboard due-list and the LLM prompt cannot disagree with the schedule, and `FOLLOWUP_MIN_COUNT` = 4 is the written run length, which `space_offsets` may now truncate; the nudge loops read their own `len(offsets)` rather than "at least four". Absolute offsets mean a touch deferred by the nightly contact window cannot push the later ones past the close — the old cumulative cadence (COLD 4+6+6+6 = 22h + jitter) overshot the 24h window and silently retired leads on three. **A reply resets the counter to zero** (`Appointment.reset_followup_sequence`, called from `mark_customer_response` and from the emailed-reply stamp in `process_inbound_emails`): the run is four touches SINCE THEY LAST SPOKE, so a lead who answers is never retired, and a reply revives one the cron already retired since it reopens the window too. It sets fields only — the caller saves. `last_followup_sent` is deliberately NOT cleared: it is the durable "have we ever chased this lead" record (the webhook's `mid_conversation` check reads it, since the counter no longer survives a reply). **Ad leads no longer get a bigger COUNT** — the old six-touch `CTWA_FOLLOWUP_OFFSETS` / `has_extended_window` path is gone; the longer window buys spacing, not extra messages. **Placement is reconciled with SENDABLE hours** — the messaging window intersected with `CONTACT_WINDOWS` (08:03-20:33, one window, every day, half-open so the last send is 20:32, SAST; owner rule 2026-09-21, replacing the 2026-09-06 blocks 12:33-14:33 and 16:02-19:33; the TEST 0 window cases derive from `CONTACT_WINDOWS` rather than retyping it, and `plan_quote.in_contact_window` reads the same constant, so the owner moves these hours in ONE place): the sendable span no longer shortens the SCHEDULE (`_sendable_hours` and `MIN_SENDABLE_SPAN_HOURS` are gone — shrinking the span dropped a whole touch where moving it would do), instead `_scheduled_due_at` rolls a due moment forward out of the quiet hours and, when that roll would land after the window has shut, pulls it BACK to `_last_sendable_moment` minus `LAST_CALL_GRACE_MINUTES` (`_window_moment_before` is the mirror of `_next_window_open`). Without that pull-back a touch due at 02:00 on a window closing at 06:00 waited for 08:21, by which time free-form sending was dead — so it only reached the lead when they messaged again, arriving as a stale nudge on top of their live message. A last call no longer relaxes the spacing (`LAST_CALL_MIN_GAP_HOURS` == the floor): what gives in that stretch is the TOUCH, dropped rather than squeezed. Two guards keep a touch off a live exchange: `FOLLOWUP_LIVE_CONVERSATION_MINUTES` (the lead just wrote — the bot's own reply is the touch; this also replaces the old 2-minute eligibility prefilter) and `FOLLOWUP_QUIET_AFTER_OUTBOUND_HOURS` (we just wrote). The delay-ghost and parked nudge loops share the last-call rule via `_is_last_call`. Delay-flow and parked nudges use the same rule (`_DELAY_NUDGE_FRACTIONS`, `_PARKED_NUDGE_FRACTIONS`), go through the same `space_offsets`, and are capped by the same cross-loop ceiling — parked nudges moved from 3/7 DAYS (always outside the window, so every one bounced 131047) into the back half of the window. A closed window still blocks everything: we never pay for templates. Other Railway crons: `send_reminders`, `send_job_reminders`, `summarize_unconfirmed_leads`, `process_inbound_emails`, `send_scheduled_followups`. `send_scheduled_followups` also carries the platform billing reminders (`bot/billing_reminders.py`, see billing.md) in their own try block. **Every service in the Railway project shares the repo's `railway.json`, whose `deploy.startCommand` overrides each service's own** — so all of them boot `start.sh`, which runs `manage.py <job>` when `PLUMBOT_CRON` is set (comma-separated for a service carrying several jobs) and gunicorn when it isn't. Adding a cron = new service + `cronSchedule` + a `PLUMBOT_CRON` value, never a start command in the dashboard (it would be overridden). The `_cronsReference` block in `railway.json` documents the current mapping (services: `Follow_Ups`, `Reminders`, `Email_Follow_Ups`, and the `Plumbot_v1` web role); Railway does not read it. **A Railway deployment bakes in the cron schedule the service had when that deployment was created.** One built while the schedule was absent comes out `buildOnly=false` / `cronSchedule=null`: it runs `start.sh` once at deploy time and never ticks again, while the service page still shows the schedule and Railway keeps advancing `nextCronRunAt` — silent, with the deployment sitting at SUCCESS. That stopped `send_followups` dead from 2026-08-25 to 2026-08-28; the only follow-ups sent in between came from a human pressing the dashboard's manual follow-up check (`manual_followup_check`), which runs the command inside the WEB service. `railway redeploy` does not fix it — it replays the bad snapshot; re-apply the schedule (`serviceInstanceUpdate`) then deploy fresh (`serviceInstanceDeployV2`). To check a cron service is genuinely alive, read its latest deployment meta (`buildOnly` true, `serviceManifest.deploy.cronSchedule` set) and confirm its `deploymentLogs` carry one `[start.sh] cron job` line per tick — the DB alone cannot tell a dead cron from a quiet one. See `_cronScheduleTrap` in `railway.json`. (`notify_priority_leads` — the daily plumber WhatsApp alert — was removed 2026-07-08; the priority-leads dashboard pages remain.)

### The plumber handoff and the job-date ladder (handoff brief, owner 2026-09-21)
- **The plumber's own line is offered as a SHORT link on WhatsApp's own domain that the LEAD taps** (`bot/plumber_link.py`). The plumber runs on a separate WhatsApp Business number with no template protection, so he must never open a cold thread: every conversation there starts inbound. **The link has to look safe** (owner, 2026-09-21): Facebook-ad leads have their guard up about scams, so never our domain and never a shortener. The only link that is short, wa.me and pre-filled is the plumber's WhatsApp Business "short link" (`wa.me/message/...`), made in HIS app (Business tools, Short link) with his own default message; his number is on the app, not the Cloud API, so we cannot make it. Stored per tenant as `TenantProfile.scripts['plumber_quote_link']` (read by `TenantConfig.plumber_quote_link`, only wa.me/api.whatsapp.com values accepted). Until one is set (owner, 2026-09-21): `wa.me/<number>` carrying the PER-LEAD pre-fill (`lead_voice_message`: service, area, one line of job, then the FIXED "I can send measurements, photos, and a plan" line), about 330 characters. A per-lead plumber override outranks the tenant's short link. **The plumber is NOT emailed on a handoff** (owner, 2026-09-21: "don't send email to plumber for every hand off message sent"; a per-handoff heads-up existed briefly and was removed, function and all). The per-lead pre-fill is what tells him who is messaging. The call brief at job - 2 is a separate, deliberate email and stays. **The handoff follow-up** is the owner's own copy, laid out as the owner asked: `HANDOFF_LEAVE_IT_HERE` ("I've messaged a couple of times, so I'll leave this here..."), `HANDOFF_WHO_HANDLES_QUOTES` ("[plumber name] handles the quotes. Send a few photos, measurements or a rough plan..."), then `HANDOFF_WHY_LINK_IS_LONG` (the long link already has their job details typed in, so it is the easiest way; the short link gets `HANDOFF_TAP_THE_LINK` instead), then **the link on its own line, then the plumber's number as the last line** (`HANDOFF_NUMBER_OF`), so the copy reads before a long URL does and they can save or message the number themselves. It names the tenant's own plumber, else its business name, never "the plumber"; "he'll send" became "you'll get" because nothing tells us the named person's pronoun. **The business-domain short link** (`bot/short_links.py`, owner 2026-09-21) sits between the two: `https://wa.<business domain>/q/<code>`, about 40 characters, forwarding to the per-lead wa.me link. The code is the lead id in base62 plus a 4-character HMAC (no table, cannot be walked); `bot/views/short_links.plumber_link_redirect` (public, `/q/` exempt in `TenantMiddleware`) answers 302 only when the request's host is THAT lead's tenant's `short_link_domain` (`scripts['short_link_domain']`), rebuilds the wa.me link from the lead's current details, and writes nothing (WhatsApp's preview fetcher requests it too, so a click log would count previews). Order in `quote_link`: the plumber's WhatsApp Business short link, then the business-domain link, then the long wa.me link; only the long one gets the "why it is long" line. Turning it on for a tenant: a `wa.` subdomain CNAME'd to the Railway web service, the domain added on that service and to the Railway `ALLOWED_HOSTS` variable, then the host stored in `scripts['short_link_domain']`. The stop-after-handoff check recognises a handoff by `plumber_link.is_handoff_text` (its copy or any link form), because the business-domain link carries no wa.me. Pinned by `short link` in TEST 0 and `ShortLinkTests`. The number is `plumber_contact()` (per-lead override, then the lead's own tenant); none means no link, never another tenant's. The delay flow's in-conversation paragraph is still `copy_catalog.PLUMBER_QUOTE_OFFER` (WE voice, English only), and `[PLUMBER_LINK_SENT]` keeps those replies from repeating it.
- **Rule 1, a delay to a date more than a week out** (`job_date_ladder.applies`, the complement of `_timeframe_is_near`): that date is the JOB date. The check-back stored is job - 7, and the lead is asked about it as a literal date ("Will it be okay if we follow up with you on the 14th of October? That gives you time to go over a quote..."), then the portfolio/email, as ONE turn in TWO split parts, because `finalise_outbound` keeps one question per part. "fixed" was dropped from the owner's wording because it is one tenant's promise. The link rides with the portfolio in every branch (address on file, address captured, PDF on WhatsApp). A new date in answer to the permission question re-arms the ladder instead of reading as a declined email. A week or less out keeps the old near-date rules.
- **The ladder** (`[JOB_DATE]`, `[JOB_LADDER]` step, `[JOB_LADDER_T1]`; walked by `send_followups._process_job_date_ladder`): a touch at job - 7 and job - 3 (email when we have one, else WhatsApp only when the free window is open; counted by the four-touch cap via `[JOB DATE FOLLOW-UP]`), and at job - 2, if the lead has not written since the first touch, the plumber is emailed a CALL BRIEF: the lead's details and the owner's call script, routed to B1 (a quote was SENT) or B2 (none) automatically. A reply after the first touch closes the ladder; a booking takes the lead off it; a cron back from an outage fires only the latest due step. Steps advance even when no channel could carry a touch, because for a no-email lead with a shut window the call is the only reach left. `_process_delayed_reactivations` skips ladder leads (their stored check-back IS job - 7), except the near-term `[DELAY_KIND]` check-ins. Pinned by `job ladder` in TEST 0 and `bot/test_handoff.py`.
- **TWO follow-ups, not four, for a lead who gets the handoff, and the handoff is the LAST text** (owner rule, 2026-09-21). `followup_offsets_for` cuts the schedule to `HANDOFF_TOUCHES` = 2 when `handoff_eligible(lead)` (the three fields plus a plumber number for the lead's own tenant; the same test `_handoff_touch` makes), so the cron, the UI chip, the due-list and retirement all read two; the delay and parked loops cap `max_nudges` at 2 whenever there is a plumber number (no field requirement for delay-signal leads). After a handoff (`handoff_sent_since_last_reply`) nothing else texts the lead until they reply: the three nudge loops stop, the job-date ladder's -7/-3 touches are held (the step still advances, so the plumber's internal call brief at -2 still goes), and `_process_delayed_reactivations` skips the lead. Pinned by `TwoFollowupsAndSilenceTests`.
- **The SECOND automatic touch of a silence is the plumber handoff, then nothing until they reply** (owner rule, 2026-09-21, superseding "the link goes once"). Two groups. (1) **A lead who gave a delay signal** (the delay-nudge loop, any `delay_*` step, and the parked loop for soft brush-offs): nudge 1 is the contextual nudge for the step they stopped at (timeframe, confirm or email); nudge 2 is `plumber_link.handoff_message(lead)` whether or not they gave an email, a date or any field. (2) **A lead with all three fields** (service type, description, area) in the main loop: follow-up 1 is the owner's script, follow-up 2 the handoff (`Command._handoff_touch`, `HANDOFF_ATTEMPT` = 2). After it, `handoff_sent_since_last_reply` (a PROACTIVE transcript entry since their last reply that carries a wa.me link; a link in a conversational reply does not count) stops all three loops and `next_followup_due_at` shows nothing due. **A reply resets every count**: the main loop through `reset_followup_sequence`; the delay and parked loops by reading a nudge stamped before the lead's latest message as belonging to the previous silence (no write on the reply; the parked loop used to stop for good once a lead replied). The handoff goes out on every cycle's second touch, even to a lead who had the link before; `[PLUMBER_LINK_SENT]` now only keeps the delay flow's in-conversation portfolio replies from repeating it. No plumber number for the lead's tenant means the ordinary nudge or script goes instead. Also fixed with it: nudge bodies joined after "Hi there," kept their capital (`_greet`), and the pre-fill takes only the first line of the stored description and drops chat (bot/job_text), so "Ok / Now you are talking" or "toilet, Ruwa / How much" is never put in the lead's mouth. Pinned by `plumber link` in TEST 0 and `SecondFollowupHandoffTests`.
- **A lead who asks about, or is wary of, the link gets an answer and the plumber's contact card** (owner, 2026-09-21; the business-domain shortener above is PARKED, built but switched off, until the owner wants it). Webhook STEP 0-, before the multi-intent composer and the delay flow's pending steps: `plumber_link.asks_about_the_link` (deterministic, two gates: one of our last 4 messages carried the link, per `is_handoff_text`, AND the message names the link and asks about it, or voices the worry on its own: "is this a scam", "is this safe", "I'm not clicking that", Shona "link iyi chii?"). The reply is `link_explanation` (catalog `LINK_WHAT_IT_IS` + `LINK_WHY_LONG` + `LINK_OR_MESSAGE_DIRECTLY`: what it opens, that it is long only because their job details are typed into it, nothing downloads, and the plumber's number to message instead; "why long" is dropped for a short link), then `_send_reply_then_contact_card` sends a WhatsApp CONTACT CARD (`WhatsAppCloudAPI.send_contact_card`, type "contacts" with `wa_id` so WhatsApp shows Message) UNDER the text, only when the text's transcript entry got `sent_at` (so a superseded or intercepted reply never leaves a lone card), recording its WAMID with `record_sent_media` as `[CONTACT CARD] name +number`. English only. Pinned by `link question` in TEST 0 and `LinkQuestionTests`.
- **A lead who agreed a check-back date is not chased before it, and one run never sends a lead two messages** (homebase 1233, 2026-09-21: deferred to "mid next month", check-back Thursday 15 October, took the portfolio on WhatsApp, said "Thank you ndaiwona", then at 18:05 got a portfolio check-in AND an `[AUTO FOLLOW-UP]` "What exactly needs doing?" four seconds apart). Three holes, three fixes. (1) The near-term portfolio/access check-in overwrites `delay_followup_due_at` with its own moment, so after it went out the column sat in the past and the main loop's `delay_followup_due_at__gt=now` exclusion no longer saw the deferral; the agreed date survives only as `[FOLLOW_UP_DATE] YYYY-MM-DD`. `out_of_scope_handler.has_agreed_checkback` now also reads the latest `[FOLLOW_UP_DATE]` still after today, and `_is_ready_for_followup` and `next_followup_due_at` hold the lead on it (the day itself belongs to send_reminders' DELAYED LEAD FOLLOW-UPS). (2) The check-ins were missing from `PROACTIVE_MARKERS`, and the cron never stamps `last_outbound_at`, so the quiet-after-outbound gap could not see a message an earlier loop of the same run had sent; the gap now also reads `last_proactive_at` (the newest proactive transcript stamp). (3) The portfolio check-in asks "hope you got a chance to look through the portfolio"; a lead who has written since it went out has answered that, so `replied_since_portfolio` (`[PDF_CHECKIN_FROM]` stamped at scheduling, else `last_outbound_at`) retires the check-in without sending. Pinned by `AgreedCheckbackHoldsFollowupsTests`.
- **A dead follow-up cron is reported, not discovered** (`bot/cron_health.py`, 2026-09-22). The schedule trap hit Follow_Ups again on 2026-09-21 12:48 (deployment `3a439b7b`, `buildOnly=false`, `cronSchedule=null`): about 20 hours with no automatic follow-ups, found only because the owner had to press "Run check". Now the SCHEDULED `send_followups` run writes a heartbeat (a `TenantSetting` row on the seed tenant, key `cron_heartbeat:send_followups`) at the very start of every run, outside every early return; only when `PLUMBOT_CRON` names it, because "Run check" runs the same command in the web service and must not make a dead cron look alive. The Email_Follow_Ups cron (a different service, so the check cannot die with the thing it checks) calls `alert_if_stale` every 5 minutes: inside the sending hours, past a 30-minute grace after they open, a heartbeat older than 30 minutes sends ONE email to `PLATFORM_NOTIFICATION_EMAIL` per outage (the row remembers which heartbeat it reported; the next beat re-arms it). The Follow-ups page shows "Auto follow-ups last ran N minutes ago" beside Run Check, red when stale. Pinned by `bot/test_cron_health.py`.
- **A "please call this lead" email gets ONE reminder 24 hours later if the call was not logged** (owner, 2026-09-22; `bot/call_brief.py`). The call email to the plumber is script first, lead details last, and ends with a "Log the call" button that opens the lead's phone-quote form (`phone_quote.form_url`, tokenized, no login). No new state: the `SentEmail` log is the record. A sent/delivered/opened email whose subject starts `Please call today: ` and that went out 24 to 72 hours ago, with no `Reminder: please call ` row logged for that lead since, is due unless the form was answered (`PhoneQuoteRequest.form_completed_at`), the lead wrote in after it, or `phone_quote.lead_is_done` (booked, closed, suppressed). The reminder is BUILT FROM the stored email, so it repeats the exact script the owner approved: the first paragraph becomes "we didn't hear back after yesterday's email" ("our email on <day>" when it was not yesterday), and "Would <day> or <day> suit you better?" moves to the next two days the tenant works. Runs from the `send_scheduled_followups` COMMAND (the Email_Follow_Ups cron, every 5 minutes), inside `CONTACT_WINDOWS`. It is deliberately not in `dispatch_due_scheduled_followups`, which `send_followups` also calls on its own cron, so two crons can never both send it. Older than 72 hours is dropped as stale. Pinned by `bot/test_call_brief.py`. The call emails themselves are still written and sent by hand (the first was Barmak lead 1162, 2026-09-22); the automatic "they promised to get back to us and did not" brief is designed but not built.
- **Not built yet** from the same brief: the price-objection line in the post-quote follow-up email (the brief flags it as unconfirmed), and the three edge cases (price-shock silence after a quote, a tenant needing landlord sign-off, the day-25 win-back on an expiring quote). The quote-to-booking sequence it asks for already exists (`start_quote_followups`, see post-visit-and-plan-path.md).

### Cancelling: a scheduled appointment, and a lead's follow-ups
- **`[FOLLOWUPS_OFF]` is the staff stop, and it is one tag read by every proactive send path.** The controls existed only on the lead's own page, and the one that claimed to stop the AUTOMATION - `pause_auto_followup` - wrote `manual_followup_paused` / `manual_followup_paused_until`, **columns migration 0018 REMOVED**, so it set plain Python attributes `save()` never persisted: the button reported success and stopped nothing (it was pinned as an `expectedFailure` for exactly that). It now writes a lead-level tag alongside the ones already there, added to `post_visit.SUPPRESSED_TAGS` (which `plan_quote` and `visit_proposal` extend) and to `send_followups._exclude_suppressed_states` plus the two nudge loops' own exclude chains - **no new column and no new resolver**, because the decision has to live on the LEAD or every send path re-litigates it. Deliberately NOT `[STOP_REQUESTED]`: that one means the CUSTOMER asked us to stop, is never cleared automatically and also sets `is_lead_active=False`, while this is our own decision and the same staff member puts it back (`Appointment.pause_followups` / `resume_followups` / `followups_paused`). **No duration** - a timed pause needs a column, and the ask was "stop, and I will put it back". The bot still answers if they write; only the proactive sends stop. Resuming fires no burst: the schedule is measured from the lead's last message, so a lead who went quiet is already spent. `get_upcoming_emails` marks a paused lead's unsent rows `stopped` rather than dropping them - they are what Restart would put back - and the dashboard's upcoming/overdue lists read the status, so they fall out of both.
- **Cancel is POST, everywhere, and it returns you to the screen you pressed it on.** `cancel_appointment` was a plain `<a href>`, which a browser can prefetch; it is `@require_POST` now and the detail page posts through the surrounding form's CSRF token via `formaction`, the same trick Complete and Delete use (a nested `<form>` there would be invalid HTML). `_followup_redirect` and `cancel_appointment` both honour a validated `next` through `safe_return_path` (the quote views' own resolver - only the local PATH survives, so a value that reached the form cannot redirect off-site), because raising or stopping something from the diary or the follow-ups dashboard and landing on a lead page you were not reading loses your tab, window and filters.
- **A queued row is cancelled; a projected one can only be STOPPED.** The follow-ups dashboard's Upcoming lists mix two things: `ScheduledFollowup` rows a person typed and queued, and touches the automation PROJECTS from lead state (the WhatsApp schedule, the post-visit / plan-quote / visit-proposal sequences), which are not rows and have nothing to cancel one at a time. So the row carries `id` when it is a real row and the template branches on it: `fd_row_cancel.html` for a queued row, `fd_row_stop_followups.html` otherwise, and the latter is also on every lead row in the Leads tab and on the lead's own WhatsApp follow-ups tab as the master switch. **Each control's `<form>` is a SIBLING of the row's `<a>`, never a child** - a form inside an anchor is invalid HTML and the browser unnests it, dropping the button out of the row - hence the `.fd-rowwrap` / `.pb-sched-line` flex wrappers, which also carry the margin the row used to have. Pinned by `CancelControlsTests`.

### Automated customer email waits for EMAIL_WINDOWS (owner rule, 2026-09-22)
- **12:30-13:30 and 18:00-19:30 SAST**, `send_followups.EMAIL_WINDOWS`, read through `plan_quote.in_email_window` (the same one-definition pattern as `CONTACT_WINDOWS`). Held: the delay reactivation and last-check emails (the whole touch waits when the lead has an email, so the WhatsApp and email legs still land together; the WhatsApp-only portfolio/access check-ins are not held), the job-ladder email touch (held BEFORE `advance()`), the dated check-back in `send_reminders`, and the post-visit confirmation and asks. Not held, because holding makes them wrong: a reply to the lead's own email, a portfolio they just asked for, appointment-time reminders, and internal mail to the plumber. A held send stamps nothing, so the next 5-minute tick inside a window sends it. Pinned by `EmailWindowTests`; tests that exercise one of those sends rather than its hour call `open_email_window(self)`.
- **Why:** the dated check-back (`send_reminders`, DELAYED LEAD FOLLOW-UPS) matched `[FOLLOW_UP_DATE] <today>` with no hour gate, so it fired on the first tick of the date: barmak 1005 and 1008 got "Following Up" emails at 00:01. Its WhatsApp leg now also waits for `CONTACT_WINDOWS`.
- **The dated check-back reads the CURRENT date, stamped per date.** It used to match ANY `[FOLLOW_UP_DATE]` line, so a lead who moved from 20 Sep to 22 Dec was sent on 20 Sep (1005), and the single bare `[FOLLOW_UP_SENT]` then blocked 22 Dec. Now `_current_followup_date` is the LAST line written (a date moved earlier is still the current one), the stamp is `[FOLLOW_UP_SENT] <date>`, a legacy bare stamp covers only dates up to 2026-09-22, and a missed date catches up for `FOLLOW_UP_CATCHUP_DAYS` (3). Pinned by `FollowUpDateCheckbackTests`.
- **A lead with no email and a shut window gets nothing on its check-back date** (846, 863, 878 in Sep 2026): no free channel, and we never pay for templates. Open question for the owner.


### Job-date ladder: the emails and the call (owner, 2026-09-22)
- **The -7 and -3 EMAILS have their own copy** (`copy_catalog.LADDER_EMAIL_FIRST` / `_SECOND`, built by `job_date_ladder.touch_email`): they say back that the lead told us they would be going ahead around their date, sound considerate rather than chasing, and offer a free quote. That means the ONLINE quote (photos, measurements, a plan), which is free for every tenant; never the visit, which some tenants charge for. A **Get quote** button (`customer_emails._get_quote_button`) sits above WhatsApp and Call and opens `plumber_link.quote_link` (the handoff's pre-filled WhatsApp to the plumber), else the tenant's own WhatsApp with the same pre-fill, else no button. The WhatsApp touches (`touch_message`) keep their own short copy. Pinned by `LadderEmailCopyTests`.
- **The -2 call email uses the owner's call-email layout** (`call_brief.build_call_email`: script first, "If ..." branches, Log the call button, details last; subject `Please call today: `, so the 24-hour call reminder covers it). Three things the old brief got wrong, each a reason the plumber had to check facts: it said the lead "had not answered our two check-ins" when a no-email lead with a shut window was sent NONE (`_checkins_sent` counts touches from the transcript, because the ladder step advances even when nothing could be sent); it put details before the script; and its no-quote branch promised "a free quote there and then" to every tenant (`_visit_offer` prices the visit by the lead's own tenant: Barmak's US$10 call-out, waived if they go ahead). Pinned by `LadderCallBriefTests` and the `job ladder` call-brief cases in TEST 0.
- **A lead who turns down email gets the portfolio AND the plumber handoff on WhatsApp; a job-date lead with no email is then ASKED about a call (owner, 2026-09-22).** Sequence: date, email ask, "no thanks" -> the PDF goes out, then `out_of_scope_handler._portfolio_on_whatsapp_ack` sends, as separate messages: "That's fine, we've sent the portfolio here."; the handoff (`plumber_link.portfolio_handoff`: who handles our quotes, what the pre-filled link opens and why it is long, the link, the number last); and ONLY for a job-date lead with no email, "Would it be okay if we called you on the 19th of October, just to see if you've got the help you need?" (`LADDER_CALL_ASK`, job - 2). **Short LINES, not a short message** (owner): each line one idea, the link kept. **Never a call without permission**: the answer is read by `_handle_call_permission_answer` (pending `delay_call_ok`, deterministic, short replies only; a refusal anywhere wins, then yes words, "no problem" is a yes, then a plain leading no). An explicit no stamps `[JOB_NO_CALL]` and `_ladder_call` sends the plumber NO brief; a yes stamps `[JOB_CALL_OK]`; NO ANSWER means the call goes ahead; anything else clears the wait and the normal flow answers it. Why: with no email and a WhatsApp window shut by job - 7 the ladder's check-ins cannot reach them, and a paid template was declined. The brief says whether they agreed or did not answer, that they have his number, and has an "If they already got a quote from you on WhatsApp" branch. The email ladder's Get quote button uses `plumber_link.long_quote_link` (the plumber's number with the lead's details pre-filled), never the fixed-message short link. Pinned by `LadderCallPermissionTests` and the `job ladder` ack cases in TEST 0.
- Barmak's visit is free (`consultation_fee` empty on its TenantProfile, checked 2026-09-22); a "US$10 call-out" in older Barmak transcripts predates that.

### A day within the week: two kinds of lead, and the plumber's call (owner, 2026-09-23)
- **Why:** WhatsApp permission is 24h from the lead's last message for every lead, so a lead who names a day a few days out ("Monday", said on Saturday) is outside the window by then; with no email the bot cannot reach them at all.
- **Type 1, "I'll contact you on Monday"** (`_is_self_initiated_defer`): "Okay, thanks, we'll wait to hear from you." and, still, the email ask for the portfolio (owner, 2026-09-23: `NEAR_WAIT_FOR_THEM` + `NEAR_EMAIL_FOR_PORTFOLIO`; an email on file gets the portfolio emailed at once, `NEAR_PORTFOLIO_EMAILED`). **No check-back is promised or scheduled: we wait for them.** `_near_they_will_contact` writes `[NEAR_AWAIT] <day> <set at>`, holds the main follow-up loop with `delay_followup_due_at` at the end of that day, and writes NO `[FOLLOW_UP_DATE]` or delay signal. The email answer (`delay_email` with no date) gets `NEAR_EMAIL_THANKS` and the portfolio, and schedules nothing; declining gets the portfolio on WhatsApp with the handoff, and `_keep_waiting` undoes the portfolio check-in and the park the PDF step sets up (a check-in is chasing; a park would suppress the plumber's call). **This replaces, for type 1 within the week only, the "check back at the time they named" rule** (the 2026-09-19 email + portfolio step is kept). The MORNING AFTER the day (from 08:00), if they have not written since, no quote has gone and no visit is booked, the plumber gets a call email (`bot/near_date_call.py`).
- **Type 2, "Contact me on Monday"** (`asks_us_to_contact`: contact/call/message/get back to/check in with + me/us; the object tells it from type 1): no longer read as "come on Monday" (it used to get "Monday works. What time suits you?"). No email on file: `NEAR_EMAIL_ASK` ("Okay, cool. Can I get your email?"), `[NEAR_CALL] <day> <set at>`, the check-back date stored. An email given: the check-back goes by email on the day AND the plumber still gets the call email that morning (owner, 2026-09-23: "yes, plumber should call as well"; it used to be email only). Declined or never given: the portfolio goes on WhatsApp with the handoff and `NEAR_WE_WILL_CALL` ("No problem, we'll give you a call on Monday.", no permission question, the request was theirs), and the plumber gets the call email that MORNING. An email already on file falls through to the ordinary park path (confirms the day, emails the portfolio), and the tag is already set, so the plumber calls that morning too. The deterministic deferral override also routes "contact me" + a timeframe into the delay flow.
- **The call email** (`near_date_call.build_brief`): the owner's call-email layout, script first (what they said, then "still keen, or has the timing moved?", the visit priced by the lead's own tenant, the online quote only if they're not keen on a visit), subject `CALL_SUBJECT_PREFIX` so the 24h call reminder covers it, "Log the call" to the phone-quote form. Once per lead (`[NEAR_CALL_SENT]`); skipped and marked done if they wrote since, booked, got a quote, or are suppressed (a type 2 lead with an email is called too, and the brief says our check-back email went that day); dropped if the day is more than 3 days gone. Runs every Follow_Ups tick, before the contact-hours cutoff (internal email, own 08:00 gate).
- English only, strictly (the shared detector must say `english` and no ndicha-/ndino-/ndiri- verb): "ndichakubatayi" keeps the old Shona behaviour until Shona copy is approved. More than a week out is unchanged (the job-date ladder). Pinned by `NearDateContactTests`, the `near date` cases in TEST 0 and `scenarios/self_defer.txt`.
